---
machine:
    python:
        version: 2.7.9

dependencies:
    override:
        - pip install -r requirements/development.txt

test:
    override:
        - flake8
        - coverage run -m nose
        - coveralls
